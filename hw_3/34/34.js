for ( var i = 1; i <= 9; i++ ) {
	for ( var j = 1; j <= 9; j++ ) {
		p = i * j
		console.log((i) + " x " + (j) + " = " + (p))
	if ( j === 6 ) {
			break
        }
    }
if ( i === 6) {
			break
        }
}